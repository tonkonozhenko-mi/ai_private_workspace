"""Golden-set retrieval evaluation harness.

Measures retrieval quality of an embedding model against a labelled question set,
so the choice of default embedder (P5) and the abstention floor rest on numbers,
not vibes. See ``eval/README.md`` and run with ``python -m eval.golden``.
"""
