fastapi>=0.138.0,<1.0
httpx>=0.28.1,<1.0
pathspec>=1.1.1,<2.0
pydantic>=2.13.4,<3.0
pytest>=8.0,<9.0
PyYAML>=6.0,<7.0
uvicorn[standard]>=0.29,<1.0
pyinstaller>=6.0,<7.0
python-dotenv>=1.0

# Security floors for transitive dependencies. The locked build
# (requirements.lock) already resolves safe versions; these also pin the loose
# and Docker install paths so OSV resolves them safely too.
idna>=3.15        # PYSEC-2026-215 / GHSA-65pc-fj4g-8rjx
Pygments>=2.20.0  # PYSEC-2023-117, GHSA-5239-wwwm-4pmq
